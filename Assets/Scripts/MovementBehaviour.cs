using UnityEngine;

public class MovementBehaviour : MonoBehaviour {

    static void stationary (IAgent agent) {
        
    }

    static void drift (IAgent agent) {
        // vector stuff
    }

    static void wander (IAgent agent) {
        // vector stuff
    }

    static void glide (IAgent agent) {
        // vector stuff
    }

}
