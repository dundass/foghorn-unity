using UnityEngine;

public class UserShortcuts : MonoBehaviour
{
    public Camera mainCamera;

    private bool showingWorldMap = false; // poss move to WorldRenderer

    // Update is called once per frame
    void Update()
    {
        //if(Input.GetMouseButtonDown(1)) {
            //showingWorldMap = !showingWorldMap;
            //mainCamera.orthographicSize = showingWorldMap ? 180 : 5;
        //}
    }
}
